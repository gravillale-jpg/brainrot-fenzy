import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/shop")({ component: () => <div className="py-20 text-center text-muted-foreground">Shop — скоро (Этап 5)</div> });
