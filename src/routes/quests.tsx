import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/quests")({ component: () => <div className="py-20 text-center text-muted-foreground">Quests — скоро (Этап 8)</div> });
