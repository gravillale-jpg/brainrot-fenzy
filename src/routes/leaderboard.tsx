import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/leaderboard")({ component: () => <div className="py-20 text-center text-muted-foreground">Leaderboard — скоро (Этап 8)</div> });
