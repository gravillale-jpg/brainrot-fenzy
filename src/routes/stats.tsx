import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/stats")({ component: () => <div className="py-20 text-center text-muted-foreground">Stats — скоро (Этап 8)</div> });
